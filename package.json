{
  "name": "a11y-linting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "doc"
  },
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-htmllint": "^0.0.15"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  },
  "scripts": {
  	"build": "gulp htmllint",
    "audit": "live-server --no-browser . | lighthouse http://localhost:8080 --verbose --chrome-flags='--headless' --output-path=./reports/report.html --save-assets --config-path=./reports/config/lighthouse-config.js"
  },
  "author": "",
  "license": "ISC"
}
